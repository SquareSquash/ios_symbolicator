source 'https://rubygems.org'

gem 'plist'
# version 4.0 only compatible with ruby 1.9+
gem 'activesupport', '< 4.0.0'
gem 'serialbox'
gem 'squash_uploader', '>= 1.0.1', :require => nil

group :development do
  gem 'rspec'

  gem 'yard'
  # version 3.0 only compatible with ruby 1.9+
  gem 'redcarpet', '< 3.0.0', :require => nil

  gem 'jeweler'
end
